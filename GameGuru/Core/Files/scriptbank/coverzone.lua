-- LUA Script - precede every function and global member with lowercase name of script + '_main'

function coverzone_init(e)
end

function coverzone_main(e)
 -- maybe detect when a character uses this cover location?
 Destroy(e)
end
