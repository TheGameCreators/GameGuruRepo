-- LUA Script - precede every function and global member with lowercase name of script + '_main'
-- defines a global variable


CoinCount = 0


function coincountstart_init(e)

end

function coincountstart_main(e)


end
